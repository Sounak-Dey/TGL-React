import React from "react";

function Header({name, title}){
    return (
        <div>
            <h1 className={"text-center my-5"}>
                The Gift Library
            </h1>
        </div>
    );
}

export default Header;